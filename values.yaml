# Default values for rmt.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

db:
  image:
    repository: registry.suse.de/suse/sle-15-sp3/update/bci/containerfile/suse/rmt-mariadb
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: "10.5"
  storage:
    class: local-path
    size: 100Mi
  mysql:
    database: rmt
    user: rmt
    password:
    rootPassword:
  replicaCount: 1
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 100
    targetCPUUtilizationPercentage: 80

app:
  init:
    image:
      repository: registry.suse.de/suse/sle-15-sp3/update/bci/containerfile/suse/rmt-mariadb-client
      pullPolicy: Always
      # Overrides the image tag whose default is the chart appVersion.
      tag: "10.5"
  image:
    repository: registry.suse.de/suse/sle-15-sp3/update/bci/containerfile/suse/rmt-server
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: "2.7"
  mysql:
    host:
  storage:
    class: local-path
    size: 300Mi
  scc:
    username:
    password:
  service:
    port: 4224
  cronjob:
    sync:
      schedule: "* 1 * * *"
    mirror:
      schedule: "30 2 * * *"

front:
  init:
    image:
      repository: registry.suse.com/bci/bci-micro
      pullPolicy: Always
      # Overrides the image tag whose default is the chart appVersion.
      tag: "15.3"
  image:
    repository: registry.suse.de/suse/sle-15-sp3/update/bci/containerfile/suse/rmt-nginx
    pullPolicy: Always
    # Overrides the image tag whose default is the chart appVersion.
    tag: "1.19"
  service:
    port: 80


ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: "/"
          pathType: Prefix
  tls:
  - secretName: rmt-cert
    hosts:
    - chart-example.local

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""
